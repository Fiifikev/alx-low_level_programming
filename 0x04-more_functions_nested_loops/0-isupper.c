#include "main.h"

/**
 * _isupper - check if the number is upper
 *
 * @c: number to be checked
 * Return: 1 and 0 otherwise
 */





int _isupper(int c)
{
	if (c >= 60 && c <= 90)
	{
		return (1);

	} else
		return (0);

}
